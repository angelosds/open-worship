import React from 'react';

import * as S from './styles';

const MainLayout = ({ children }) => (
  <S.MainLayout>
    Open Worship
    {children}
  </S.MainLayout>
);

export default MainLayout;
